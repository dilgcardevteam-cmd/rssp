I will modify the applicant management system to implement "Compliance" and "Qualified" statuses, updating the logic to strictly follow your new rules regarding document verification and revision needs.

### **1. Update Logic in `AdminController.php`**
*   **Modify `notifyApplicant` method:**
    *   Retrieve all documents for the applicant.
    *   **Strict Logic Check:**
        *   If **any** document has status **`Needs Revision`**, change the Application status to **`Compliance`**. (This moves them to the Compliance tab).
        *   If **all** documents have status **`Verified`**, change the Application status to **`Qualified`**. (This moves them to the Qualified tab).
    *   Save the application status **before** sending the email notification.
    *   *Note: This ensures that applicants move tabs only when the admin finalizes the review by clicking "Notify Applicant".*

### **2. Update Controller `ShowApplicantsProfile.php`**
*   **Modify `manageApplicants` method:**
    *   Fetch **New Applicants** where `status` is `'Pending'`.
    *   Fetch **Compliance Applicants** where `status` is `'Compliance'`.
    *   Fetch **Qualified Applicants** where `status` is `'Qualified'`.
    *   Pass all three datasets to the view.
*   **Add/Update AJAX Filter Methods:**
    *   Create `ajaxFilterComplianceApplicants` to handle sorting/searching for the Compliance tab.
    *   Update `ajaxFilterReviewedApplicants` (renaming to `ajaxFilterQualifiedApplicants`) to filter strictly for `status = 'Qualified'`.

### **3. Update View `manage_applicants.blade.php`**
*   **Tabs Configuration:**
    *   Rename "Reviewed Applicants" to **"Qualified Applicants"**.
    *   Insert a new **"Compliance"** tab between "New Applicants" and "Qualified Applicants".
*   **Table Structures:**
    *   **Compliance Tab:** Display applicants with `Compliance` status (columns: Name, Job, Assignment, Status, Actions).
    *   **Qualified Tab:** Display applicants with `Qualified` status, but **remove the "Status" column** entirely as requested.
*   **JavaScript Logic:**
    *   Update `switchTab` to toggle between `new`, `compliance`, and `qualified`.
    *   Implement `fetchComplianceApplicants` for the new tab.
    *   Update `fetchQualifiedApplicants` to use the new route/logic.

### **4. Routes**
*   Define new routes in `routes/web.php` for the AJAX filtering:
    *   `/admin/manage_applicants/compliance`
    *   `/admin/manage_applicants/qualified`
