I will fix the Exam Lobby fetching logic to ensure it only displays active participants (those who have entered the exam link) and correct the routing configuration.

### **1. Fix Data Fetching in** **`ExamController.php`**

* **Update** **`manageExam`** **Method:**

  * Currently, it fetches **all** applications for the vacancy (`Applications::where('vacancy_id', $vacancy_id)->get()`), which includes "Pending" and "Not Qualified" applicants.

  * **Fix:** I will apply the same filter used in `getLobbyData`:

    ```php
    $participants = Applications::where('vacancy_id', $vacancy_id)
        ->whereNotNull('read_at') // Only fetch those who have entered the lobby
        ->with('user')
        ->get();
    ```

  * This ensures the initial page load matches the auto-refresh logic and only shows relevant applicants.

### **2. Fix Route Configuration in** **`web.php`**

* **Correct Route Definition:**

  * The route `user.exam_lobby` is currently pointing to the `submit` method, which is incorrect.

  * **Fix:** I will change it to point to the correct `examLobby` method:

    ```php
    Route::get('/exam/{vacancy_id}/lobby', [ExamController::class, 'examLobby'])->name('user.exam_lobby');
    ```

### **3. Verify** **`getLobbyData`**

* I will double-check `getLobbyData` to confirm it strictly filters by `read_at` (as verified in my research), ensuring consistency between the initial view and AJAX updates.

