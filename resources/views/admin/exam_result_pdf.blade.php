<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Examination Result</title>

<style>
    body {
        font-family: "Times New Roman", Times, serif;
        font-size: 13px;
        color: #000;
        margin: 40px;
        line-height: 1.6;
    }

    .document {
        max-width: 800px;
        margin: auto;
    }

    .center {
        text-align: center;
    }

    .header-title {
        font-size: 16px;
        font-weight: bold;
        text-transform: uppercase;
        margin-bottom: 2px;
    }

    .sub-header {
        font-size: 14px;
        margin-bottom: 20px;
    }

    .divider {
        border-top: 2px solid #000;
        margin: 10px 0 20px 0;
    }

    table.meta-table {
        width: 100%;
        border-collapse: collapse;
        margin-bottom: 20px;
    }

    .meta-table td {
        padding: 4px 6px;
        vertical-align: top;
    }

    .meta-label {
        width: 220px;
        font-weight: bold;
    }

    .section-title {
        font-weight: bold;
        text-transform: uppercase;
        margin: 25px 0 10px 0;
        border-bottom: 1px solid #000;
        padding-bottom: 3px;
    }

    .question {
        margin-bottom: 20px;
        page-break-inside: avoid;
    }

    .question-number {
        font-weight: bold;
    }

    .question-text {
        margin-bottom: 6px;
    }

    .points {
        float: right;
        font-size: 12px;
    }

    .answer-box {
        border: 1px solid #000;
        min-height: 110px;
        padding: 8px;
        white-space: pre-wrap;
    }

    .certification {
        margin-top: 40px;
        font-size: 12px;
    }

    .signature-block {
        margin-top: 50px;
        width: 300px;
    }

    .signature-line {
        border-top: 1px solid #000;
        margin-top: 40px;
        padding-top: 3px;
        font-size: 12px;
    }

    @media print {
        body {
            margin: 20mm;
        }
        .no-print {
            display: none !important;
        }
    }
</style>
</head>

<body>
<div class="document">

    <div class="center">
        <div class="header-title">Republic of __________</div>
        <div class="header-title">Civil Service Commission</div>
        <div class="sub-header">Examination Result Report</div>
    </div>

    <div class="divider"></div>

    <table class="meta-table">
        <tr>
            <td class="meta-label">Examinee Code:</td>
            <td>{{ $examineeCode }}</td>
        </tr>
        <tr>
            <td class="meta-label">Position Applied For:</td>
            <td>{{ $positionTitle->position_title ?? '-' }}</td>
        </tr>
        <tr>
            <td class="meta-label">Date of Examination:</td>
            <td>{{ optional($examDetail)->date ? \Carbon\Carbon::parse($examDetail->date)->format('F d, Y') : '-' }}</td>
        </tr>
        <tr>
            <td class="meta-label">Time Started:</td>
            <td>{{ optional($application->exam_started_at)->format('g:i A') ?? '-' }}</td>
        </tr>
        <tr>
            <td class="meta-label">Time Submitted:</td>
            <td>{{ optional($application->exam_end_time)->format('g:i A') ?? '-' }}</td>
        </tr>
        <tr>
            <td class="meta-label">Report Extracted At:</td>
            <td>{{ $extractedAt->format('F d, Y g:i A') }}</td>
        </tr>
    </table>

    <div class="section-title">Examination Responses</div>

    @foreach ($examItems as $idx => $item)
        <div class="question">
            <div class="question-text">
                <span class="question-number">
                    {{ ($idx + 1) . '. ' }}
                </span>
                {{ $item->question }}

                @if ($item->is_essay)
                    <span class="points">
                        (Maximum: {{ $item->essay_max_score ?? 0 }} points)
                    </span>
                @endif
            </div>

            <div class="answer-box">
                @php
                    $ans = $answers[$item->id] ?? null;
                @endphp
                {!! nl2br(e($ans ?? '')) !!}
            </div>
        </div>
    @endforeach

    <div class="certification">
        <p>
            This document is an official record generated by the Examination Management System.
            It is valid without signature unless otherwise required for administrative purposes.
        </p>
    </div>

    <div class="signature-block">
        <div class="signature-line">
            Authorized Officer / Examiner
        </div>
    </div>

    <div class="no-print" style="margin-top:20px;">
        <button onclick="window.print()">Print / Save as PDF</button>
    </div>

</div>
</body>
</html>